/**
 * Knetik Platform API Documentation latest 
 * This is the spec for the Knetik API.  Use this in conjunction with the documentation found at https://knetikcloud.com.
 *
 * OpenAPI spec version: latest 
 * Contact: support@knetik.com
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Inject, Injectable, Optional }                      from '@angular/core';
import { HttpClient, HttpHeaders, HttpParams,
         HttpResponse, HttpEvent }                           from '@angular/common/http';
import { CustomHttpUrlEncodingCodec }                        from '../encoder';

import { Observable }                                        from 'rxjs/Observable';

import { ActionResource } from '../model/actionResource';
import { Result } from '../model/result';

import { BASE_PATH, COLLECTION_FORMATS }                     from '../variables';
import { Configuration }                                     from '../configuration';


@Injectable()
export class RuleEngineActionsService {

    protected basePath = 'https://jsapi-integration.us-east-1.elasticbeanstalk.com';
    public defaultHeaders = new HttpHeaders();
    public configuration = new Configuration();

    constructor(protected httpClient: HttpClient, @Optional() configuration: Configuration) {
        if (configuration) {
            this.configuration = configuration;
            this.basePath = configuration.basePath || this.basePath;
        }
    }

    /**
     * @param consumes string[] mime-types
     * @return true: consumes contains 'multipart/form-data', false: otherwise
     */
    private canConsumeForm(consumes: string[]): boolean {
        const form = 'multipart/form-data';
        for (const consume of consumes) {
            if (form === consume) {
                return true;
            }
        }
        return false;
    }


    /**
     * Get a list of available actions
     * &lt;b&gt;Permissions Needed:&lt;/b&gt; BRE_RULE_ENGINE_ACTIONS_USER
     * @param filterCategory Filter for actions that are within a specific category
     * @param filterName Filter for actions that have names containing the given string
     * @param filterTags Filter for actions that have all of the given tags (comma separated list)
     * @param filterSearch Filter for actions containing the given words somewhere within name, description and tags
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     */
    public getBREActions(filterCategory?: 'achievement' | 'behavior' | 'comment' | 'disposition' | 'device' | 'entitlement' | 'friends' | 'fulfillment' | 'gamification' | 'inventory' | 'invoice' | 'media' | 'scheduler' | 'store' | 'subscription' | 'user' | 'wallet' | 'custom' | 'challenge' | 'activity' | 'campaign' | 'event', filterName?: string, filterTags?: string, filterSearch?: string, observe?: 'body', reportProgress?: boolean): Observable<Array<ActionResource>>;
    public getBREActions(filterCategory?: 'achievement' | 'behavior' | 'comment' | 'disposition' | 'device' | 'entitlement' | 'friends' | 'fulfillment' | 'gamification' | 'inventory' | 'invoice' | 'media' | 'scheduler' | 'store' | 'subscription' | 'user' | 'wallet' | 'custom' | 'challenge' | 'activity' | 'campaign' | 'event', filterName?: string, filterTags?: string, filterSearch?: string, observe?: 'response', reportProgress?: boolean): Observable<HttpResponse<Array<ActionResource>>>;
    public getBREActions(filterCategory?: 'achievement' | 'behavior' | 'comment' | 'disposition' | 'device' | 'entitlement' | 'friends' | 'fulfillment' | 'gamification' | 'inventory' | 'invoice' | 'media' | 'scheduler' | 'store' | 'subscription' | 'user' | 'wallet' | 'custom' | 'challenge' | 'activity' | 'campaign' | 'event', filterName?: string, filterTags?: string, filterSearch?: string, observe?: 'events', reportProgress?: boolean): Observable<HttpEvent<Array<ActionResource>>>;
    public getBREActions(filterCategory?: 'achievement' | 'behavior' | 'comment' | 'disposition' | 'device' | 'entitlement' | 'friends' | 'fulfillment' | 'gamification' | 'inventory' | 'invoice' | 'media' | 'scheduler' | 'store' | 'subscription' | 'user' | 'wallet' | 'custom' | 'challenge' | 'activity' | 'campaign' | 'event', filterName?: string, filterTags?: string, filterSearch?: string, observe: any = 'body', reportProgress: boolean = false ): Observable<any> {

        let queryParameters = new HttpParams({encoder: new CustomHttpUrlEncodingCodec()});
        if (filterCategory !== undefined && filterCategory !== null) {
            queryParameters = queryParameters.set('filter_category', <any>filterCategory);
        }
        if (filterName !== undefined && filterName !== null) {
            queryParameters = queryParameters.set('filter_name', <any>filterName);
        }
        if (filterTags !== undefined && filterTags !== null) {
            queryParameters = queryParameters.set('filter_tags', <any>filterTags);
        }
        if (filterSearch !== undefined && filterSearch !== null) {
            queryParameters = queryParameters.set('filter_search', <any>filterSearch);
        }

        let headers = this.defaultHeaders;

        // authentication (oauth2_client_credentials_grant) required
        if (this.configuration.accessToken) {
            const accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers = headers.set('Authorization', 'Bearer ' + accessToken);
        }

        // authentication (oauth2_password_grant) required
        if (this.configuration.accessToken) {
            const accessToken = typeof this.configuration.accessToken === 'function'
                ? this.configuration.accessToken()
                : this.configuration.accessToken;
            headers = headers.set('Authorization', 'Bearer ' + accessToken);
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers = headers.set('Accept', httpHeaderAcceptSelected);
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];

        return this.httpClient.get<Array<ActionResource>>(`${this.basePath}/bre/actions`,
            {
                params: queryParameters,
                withCredentials: this.configuration.withCredentials,
                headers: headers,
                observe: observe,
                reportProgress: reportProgress
            }
        );
    }

}
